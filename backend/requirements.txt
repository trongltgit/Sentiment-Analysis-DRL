# Web Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Deep Learning (chỉ cần thiết)
torch==2.1.0
transformers==4.35.0

# Data Processing
numpy==1.24.3

# Utilities
pydantic==2.5.0
python-dotenv==1.0.0

# BỎ: selenium, beautifulsoup4, stable-baselines3, gymnasium, pandas, scikit-learn, nltk, textblob, sqlalchemy, alembic, redis, celery
# (Vì hiện tại đang dùng mock data, không cần crawl thật)
